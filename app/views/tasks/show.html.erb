<h1><%= @task.name %></h1>

<p>Deadline is on: <%= @task.deadline %></p>

<p>This task is done: <%= @task.done %></p>


<%= link_to "Edit", edit_task_path(@task) %>
<%= link_to "Delete", task_path(@task), method: :delete, data: { confirm: "Are you sure "} %>
<%= link_to "Back to list", tasks_path %>
